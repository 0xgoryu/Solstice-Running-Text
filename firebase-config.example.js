// Copy this file to "firebase-config.js" and fill in your own Firebase
// project credentials. Get these values from:
// Firebase Console → Project settings → General → Your apps → SDK setup and configuration
//
// firebase-config.js (the real file, NOT this example) should be added to
// .gitignore so your keys are never pushed to a public repo.

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
